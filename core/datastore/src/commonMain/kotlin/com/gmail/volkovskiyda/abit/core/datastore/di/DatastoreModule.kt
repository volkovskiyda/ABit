package com.gmail.volkovskiyda.abit.core.datastore.di

import org.koin.core.module.Module
import org.koin.dsl.module

/** Empty until DataStore arrives (plan item 07). See `databaseModule` for the same reasoning. */
val datastoreModule: Module = module { }
