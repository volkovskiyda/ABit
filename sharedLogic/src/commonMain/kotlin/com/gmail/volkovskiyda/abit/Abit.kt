package com.gmail.volkovskiyda.abit

/**
 * Placeholder so the module has something to compile while the real `core:*` modules are built
 * (plan item 03, which retires this module entirely).
 */
object Abit {
    const val NAME: String = "ABit"
}
